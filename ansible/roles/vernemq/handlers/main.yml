---
- name: restart vernemq
  ansible.builtin.service:
    name: vernemq
    state: restarted
  become: true
  environment:
    WAIT_FOR_ERLANG: "60"

- name: reload vernemq
  ansible.builtin.command:
    cmd: "{{ vernemq_install_dir }}/bin/vmq-admin reload"
  become: true
  changed_when: true
